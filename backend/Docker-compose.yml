version: "3.6"
services:
  postgres:
    image: postgis/postgis:16-3.4
    restart: always
    ports:
      - "5432:5432"
    volumes:
      - ~/apps/postgres:/var/lib/postgresql/data
    environment:
      POSTGRES_PASSWORD: 123
      POSTGRES_DB: pet
volumes:
  db_data:
