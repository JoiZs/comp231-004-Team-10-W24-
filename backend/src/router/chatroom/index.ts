import { Router } from "express";

const chatroomRouter = Router();

chatroomRouter.post("/create", (req, res) => {});
chatroomRouter.get("/all", (req, res) => {});
chatroomRouter.post("/resv", (req, res) => {});

export default chatroomRouter;
